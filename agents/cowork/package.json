{
  "name": "galaxia-cowork",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Galaxia Cowork orchestrator — plans a goal into a DAG, gates consequential subtasks, executes each in a disposable Docker sandbox, synthesizes a deliverable",
  "main": "orchestrator.mjs",
  "scripts": {
    "start": "node orchestrator.mjs",
    "syntax": "node --check orchestrator.mjs && node --check schema.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.6",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.23.8"
  }
}
