{
  "name": "galaxia-browser-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke test du cockpit Galaxia depuis un vrai Chromium headless (Playwright). Vérifie l'auth gate, le rendu du login, les routes protégées, /api/tts.",
  "type": "module",
  "scripts": {
    "test": "node test.mjs",
    "install:browser": "playwright install chromium",
    "install:deps": "playwright install-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
